import { useEffect, useRef, useState } from "react";

// Pi Network integration removed.
// This component is retained as a no-op placeholder so the UI build remains
// intact. Payment and authentication features have been removed from the
// repository to avoid embedding Mainnet/Testnet keys or Pine SDK usage.

export function PiAuth() {
  // No-op: Pi authentication disabled in this build.
  return null;
}
